<UserControl x:Class="Scada.MonitorModule.Views.MonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1100"
             Background="#1E1E82">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>
        <!-- 左侧组态画面-->
        <Border Grid.Column="0" Background="#252526"
                BorderBrush="#3F3F46" BorderThickness="1" CornerRadius="4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="35" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Border Background="#2D2D30" CornerRadius="4,4,0,0">
                    <TextBlock Text="工艺流程监控" Margin="10,0"
                               VerticalAlignment="Center" FontWeight="Bold" />
                </Border>
                <!-- 组态画布-->
                <Canvas Grid.Row="1" x:Name="ProcessCanvas">
                    <!-- 储罐-->
                    <Grid Canvas.Left="100" Canvas.Top="80"
                          Width="120" Height="200">
                        <Rectangle Stroke="#007ACC"
                                   StrokeThickness="2" Fill="#1A1A2E" RadiusX="6" RadiusY="6" />
                        <TextBlock Text="原料储罐"
                                   HorizontalAlignment="Center" Margin="0,10,0,0" Foreground="White" />
                        <TextBlock Text="{Binding TankLevel,
StringFormat='{}{0:F2} m'}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center" FontSize="20" Foreground="#4ECDC4" />
                        <TextBlock Text="TAG001"
                                   HorizontalAlignment="Center" VerticalAlignment="Bottom"
                                   Margin="0,0,0,10" FontSize="10" Foreground="Gray" />
                    </Grid>
                    <!-- 管道-->
                    <Line X1="220" Y1="180" X2="350" Y2="180"
                          Stroke="#007ACC" StrokeThickness="4" />
                    <!-- 泵-->
                    <Grid Canvas.Left="360" Canvas.Top="160"
                          Width="60" Height="60">
                        <Ellipse Stroke="#007ACC" StrokeThickness="2"
                                 Fill="{Binding PumpStatusColor}" />
                        <TextBlock Text="泵"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   Foreground="White" />
                    </Grid>
                    <!-- 管道2 -->
                    <Line X1="420" Y1="190" X2="550" Y2="190"
                          Stroke="#007ACC" StrokeThickness="4" />
                    <!-- 反应釜-->
                    <Grid Canvas.Left="560" Canvas.Top="60"
                          Width="150" Height="240">
                        <Rectangle Stroke="#FF6B6B"
                                   StrokeThickness="2" Fill="#1A1A2E" RadiusX="10" RadiusY="10" />
                        <TextBlock Text="反应釜"
                                   HorizontalAlignment="Center" Margin="0,10,0,0" Foreground="White" />
                        <StackPanel VerticalAlignment="Center"
                                    Margin="10,0">
                            <TextBlock Foreground="#FFE66D">
                                温度:
                                {Binding ReactorTemp:F2} ℃
                            </TextBlock>
                            <TextBlock Foreground="#4ECDC4"
                                       Margin="0,5,0,0">
                                压力: {Binding ReactorPressure:F2} MPa
                            </TextBlock>
                        </StackPanel>
                    </Grid>
                    <!-- 流量显示-->
                    <StackPanel Canvas.Left="300" Canvas.Top="220">
                        <TextBlock Text="瞬时流量" Foreground="Gray" FontSize="10" />
                        <TextBlock Text="{Binding FlowRate, StringFormat='{}{0:F2} m³/h'}" Foreground="#95E1D3" FontSize="16" />
                    </StackPanel>
                </Canvas>
            </Grid>
        </Border>
        <!-- 右侧实时数据列表-->
        <Border Grid.Column="1" Background="#252526"
                BorderBrush="#3F3F46" BorderThickness="1" CornerRadius="4"
                Margin="10,0,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="35" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Border Background="#2D2D30" CornerRadius="4,4,0,0">
                    <TextBlock Text="实时数据" Margin="10,0"
                               VerticalAlignment="Center" FontWeight="Bold" />
                </Border>
                <ListView Grid.Row="1" ItemsSource="{Binding TagList}"
                          Background="Transparent" BorderThickness="0">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Background"
                                    Value="Transparent" />
                            <Setter Property="BorderThickness"
                                    Value="0,0,0,1" />
                            <Setter Property="BorderBrush"
                                    Value="#3F3F46" />
                            <Setter Property="Padding" Value="5,8" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="50" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Name}"
                                           Foreground="White" />
                                <TextBlock Grid.Column="1"
                                           Text="{Binding Value}" Foreground="#4ECDC4" FontWeight="Bold" />
                                <TextBlock Grid.Column="2"
                                           Text="{Binding Unit}" Foreground="Gray" FontSize="10"
                                           Margin="5,0,0,0" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</UserControl>